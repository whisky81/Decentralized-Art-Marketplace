{
  "name": "pinata-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.10",
    "pinata": "^2.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
